/*
Handles database manipulations with todo lists
*/
module.exports = function(app){
  var mongoose = require('mongoose');

  // Handle requests to todo page
  app.get('/todo', app.get('require')function(req, res){
    
  });
}
